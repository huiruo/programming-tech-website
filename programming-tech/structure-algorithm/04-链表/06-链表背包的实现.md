---
title: 链表背包的实现
sidebar_position: 1
---

```text
只需要将 stack 中的push() 该名为 add(),并去掉pop()的实现即可。

bag 和 stack 都是后进先出的。

示例代码维护了一条链表，用于保存所有通过 add()添加的元素。
size()和isEmpty() 和Stack中一样。

迭代器会遍历链表并将当前节点 保存到 current变量中。
```
