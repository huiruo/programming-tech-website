---
title: 关于数据结构
sidebar_position: 1
---

## 数据结构
```text
前面拥有两种表示对象集合的方式：数组和链表。
数组：通过索引可以直接访问任意元素   缺点：在初始化时候就要知道元素的数量
链表： 使用空间大小和元素数量成正比  缺点：需要通过引用访问任意元素

两者都非常基础，称为顺序存储和链式存储。

例如二叉树由 两个链接的节点组成。
另一个重要的扩展是复合型的数据结构：可以使用背包存储栈，用队列存储数组，等等。
```


## 本书所给出的数据结构 举例
```text
数据结构        抽象数据类型          数据表示

父链接树        UnionFind             整型数组

二分查找树      BST                   含有两个链接的节点

字符串          String                数组，偏移量和长度

二叉堆          PQ                    对象数组

散列表（拉链法）SeparateChaining HashSt 链表数组

散列表（线性探测法）   LinearProbingHashST  两个对象数组

图的邻接链表    Graph     Bag 对象的数组

单词查找树      TrieST    含有链接数组的节点

三向单词查早树  TST       含有三个链接的节点 
```