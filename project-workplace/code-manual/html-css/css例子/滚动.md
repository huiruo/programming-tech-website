
```
Element.scrollIntoView() 方法让当前的元素滚动到浏览器窗口的可视区域内


      /*
      foot.scrollIntoView({
        behavior: 'smooth',
        block: 'end'
      });
      */
      /*
      foot.scrollTo({
        top: 0,
        behavior: 'smooth',
        block: 'end'
      });
      */
```
